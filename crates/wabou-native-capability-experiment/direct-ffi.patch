diff --git a/apps/devtools/src/bindings.rs b/apps/devtools/src/bindings.rs
index cf5ad99..4e0ea66 100644
--- a/apps/devtools/src/bindings.rs
+++ b/apps/devtools/src/bindings.rs
@@ -1,5 +1,5 @@
 use serde::{Deserialize, Serialize};
-use wabou::{Bindings, Capability, JsonMethod, Type, specta};
+use wabou::{Bindings, Capability, HostMethod, Type, specta};
 use wabou_devtools::{DebugFrame, DebugNode, DebugOverlay, DebugStatus};

 /// Host capability containing the DevTools example endpoints.
@@ -10,24 +10,24 @@ pub mod method {
     use super::*;

     /// Select an inspected runtime socket.
-    pub const CONNECT: JsonMethod<ConnectRequest, PathResult> = JsonMethod::new("connect");
+    pub const CONNECT: HostMethod<ConnectRequest, PathResult> = HostMethod::new("connect");
     /// Read inspected runtime status.
-    pub const STATUS: JsonMethod<(), DebugStatus> = JsonMethod::no_request("status");
+    pub const STATUS: HostMethod<(), DebugStatus> = HostMethod::no_request("status");
     /// Query retained nodes.
-    pub const QUERY_NODES: JsonMethod<QueryNodesRequest, Vec<DebugNode>> =
-        JsonMethod::new("queryNodes");
+    pub const QUERY_NODES: HostMethod<QueryNodesRequest, Vec<DebugNode>> =
+        HostMethod::new("queryNodes");
     /// Inspect one retained node.
-    pub const INSPECT_NODE: JsonMethod<InspectNodeRequest, DebugNode> =
-        JsonMethod::new("inspectNode");
+    pub const INSPECT_NODE: HostMethod<InspectNodeRequest, DebugNode> =
+        HostMethod::new("inspectNode");
     /// Read recent bridge frames.
-    pub const RECENT_FRAMES: JsonMethod<RecentFramesRequest, Vec<DebugFrame>> =
-        JsonMethod::new("recentFrames");
+    pub const RECENT_FRAMES: HostMethod<RecentFramesRequest, Vec<DebugFrame>> =
+        HostMethod::new("recentFrames");
     /// Capture a native screenshot.
-    pub const CAPTURE_SCREENSHOT: JsonMethod<(), PathResult> =
-        JsonMethod::no_request("captureScreenshot");
+    pub const CAPTURE_SCREENSHOT: HostMethod<(), PathResult> =
+        HostMethod::no_request("captureScreenshot");
     /// Configure the inspected runtime overlay.
-    pub const SET_OVERLAY: JsonMethod<SetOverlayRequest, DebugOverlay> =
-        JsonMethod::new("setOverlay");
+    pub const SET_OVERLAY: HostMethod<SetOverlayRequest, DebugOverlay> =
+        HostMethod::new("setOverlay");
 }

 /// Filesystem path returned by socket and screenshot operations.
@@ -105,6 +105,5 @@ mod tests {
         assert!(output.contains("queryNodes(request: QueryNodesRequest): Promise<DebugNode[]>"));
         assert!(output.contains("selectedNode: number | null"));
         assert!(output.contains("captureScreenshot(): Promise<PathResult>"));
-        assert!(output.contains("captureScreenshot(request: string): NativeResult<string>"));
     }
 }
diff --git a/apps/devtools/src/main.rs b/apps/devtools/src/main.rs
index 3f4e988..657d277 100644
--- a/apps/devtools/src/main.rs
+++ b/apps/devtools/src/main.rs
@@ -56,7 +56,7 @@ fn main() -> Result<(), Whatever> {
                 .min_inner_size(960, 600),
         )
         .devtools(false)
-        .json_capability(CAPABILITY, move |capability| {
+        .typed_capability(CAPABILITY, move |capability| {
             let connect_target = target.clone();
             capability.method(method::CONNECT, move |connection: ConnectRequest| {
                 let target = connect_target.clone();
diff --git a/apps/devtools/ui/generated/host-bindings.ts b/apps/devtools/ui/generated/host-bindings.ts
index bfa0e98..12f68b5 100644
--- a/apps/devtools/ui/generated/host-bindings.ts
+++ b/apps/devtools/ui/generated/host-bindings.ts
@@ -1,8 +1,6 @@
 // AUTO-GENERATED by wabou-bindgen. DO NOT EDIT.
 import { type Host, useHost } from "@wabou/core";

-export type NativeResult<T> = T | PromiseLike<T>;
-
 export /**  Request selecting the inspected runtime socket. */
 type ConnectRequest = {
	/**  Native DevTools socket path. */
@@ -211,46 +209,32 @@ export type DebugFrame = DebugFrame_Serialize;

 interface NativeHostCapabilities {
   readonly devtools: {
-    captureScreenshot(): NativeResult<string>;
-    connect(request: string): NativeResult<string>;
-    inspectNode(request: string): NativeResult<string>;
-    queryNodes(request: string): NativeResult<string>;
-    recentFrames(request: string): NativeResult<string>;
-    setOverlay(request: string): NativeResult<string>;
-    status(): NativeResult<string>;
+    captureScreenshot(): Promise<PathResult>;
+    connect(request: ConnectRequest): Promise<PathResult>;
+    inspectNode(request: InspectNodeRequest): Promise<DebugNode>;
+    queryNodes(request: QueryNodesRequest): Promise<DebugNode[]>;
+    recentFrames(request: RecentFramesRequest): Promise<DebugFrame_Serialize[]>;
+    setOverlay(request: SetOverlayRequest): Promise<DebugOverlay>;
+    status(): Promise<DebugStatus>;
   };
 }

 export class NativeCapabilityError extends Error {
   readonly operation: string;
+  readonly nativeCause: unknown;

-  constructor(operation: string, message: string) {
+  constructor(operation: string, message: string, nativeCause?: unknown) {
     super(`${operation}: ${message}`);
     this.name = "NativeCapabilityError";
     this.operation = operation;
+    this.nativeCause = nativeCause;
   }
 }

-function decodeNativeResult<T>(raw: string, operation: string): T {
-  let result: unknown;
-  try {
-    result = JSON.parse(raw);
-  } catch (error) {
-    const detail = error instanceof Error ? error.message : String(error);
-    throw new NativeCapabilityError(operation, `invalid JSON response: ${detail}`);
-  }
-  if (typeof result !== "object" || result === null || typeof (result as { ok?: unknown }).ok !== "boolean") {
-    throw new NativeCapabilityError(operation, "invalid response envelope");
-  }
-  const envelope = result as { ok: true; value?: T } | { ok: false; error?: unknown };
-  if (!envelope.ok) {
-    const detail = typeof envelope.error === "string" ? envelope.error : "native capability failed";
-    throw new NativeCapabilityError(operation, detail);
-  }
-  if (!("value" in envelope)) {
-    throw new NativeCapabilityError(operation, "successful response is missing `value`");
-  }
-  return envelope.value as T;
+function asNativeCapabilityError(operation: string, error: unknown): NativeCapabilityError {
+  if (error instanceof NativeCapabilityError) return error;
+  const detail = error instanceof Error ? error.message : String(error);
+  return new NativeCapabilityError(operation, detail, error);
 }

 export interface DevtoolsClient {
@@ -266,13 +250,55 @@ export interface DevtoolsClient {
 export function createDevtoolsClient(host: Host): DevtoolsClient {
   const nativeHost = host as Host & NativeHostCapabilities;
   return {
-    captureScreenshot: async () => decodeNativeResult<PathResult>(await nativeHost.devtools.captureScreenshot(), "devtools.captureScreenshot"),
-    connect: async (request) => decodeNativeResult<PathResult>(await nativeHost.devtools.connect(JSON.stringify(request)), "devtools.connect"),
-    inspectNode: async (request) => decodeNativeResult<DebugNode>(await nativeHost.devtools.inspectNode(JSON.stringify(request)), "devtools.inspectNode"),
-    queryNodes: async (request) => decodeNativeResult<DebugNode[]>(await nativeHost.devtools.queryNodes(JSON.stringify(request)), "devtools.queryNodes"),
-    recentFrames: async (request) => decodeNativeResult<DebugFrame_Serialize[]>(await nativeHost.devtools.recentFrames(JSON.stringify(request)), "devtools.recentFrames"),
-    setOverlay: async (request) => decodeNativeResult<DebugOverlay>(await nativeHost.devtools.setOverlay(JSON.stringify(request)), "devtools.setOverlay"),
-    status: async () => decodeNativeResult<DebugStatus>(await nativeHost.devtools.status(), "devtools.status"),
+    captureScreenshot: async () => {
+      try {
+        return await nativeHost.devtools.captureScreenshot();
+      } catch (error) {
+        throw asNativeCapabilityError("devtools.captureScreenshot", error);
+      }
+    },
+    connect: async (request) => {
+      try {
+        return await nativeHost.devtools.connect(request);
+      } catch (error) {
+        throw asNativeCapabilityError("devtools.connect", error);
+      }
+    },
+    inspectNode: async (request) => {
+      try {
+        return await nativeHost.devtools.inspectNode(request);
+      } catch (error) {
+        throw asNativeCapabilityError("devtools.inspectNode", error);
+      }
+    },
+    queryNodes: async (request) => {
+      try {
+        return await nativeHost.devtools.queryNodes(request);
+      } catch (error) {
+        throw asNativeCapabilityError("devtools.queryNodes", error);
+      }
+    },
+    recentFrames: async (request) => {
+      try {
+        return await nativeHost.devtools.recentFrames(request);
+      } catch (error) {
+        throw asNativeCapabilityError("devtools.recentFrames", error);
+      }
+    },
+    setOverlay: async (request) => {
+      try {
+        return await nativeHost.devtools.setOverlay(request);
+      } catch (error) {
+        throw asNativeCapabilityError("devtools.setOverlay", error);
+      }
+    },
+    status: async () => {
+      try {
+        return await nativeHost.devtools.status();
+      } catch (error) {
+        throw asNativeCapabilityError("devtools.status", error);
+      }
+    },
   };
 }

diff --git a/apps/devtools/ui/host-bindings.test.ts b/apps/devtools/ui/host-bindings.test.ts
index e786e4b..6e386af 100644
--- a/apps/devtools/ui/host-bindings.test.ts
+++ b/apps/devtools/ui/host-bindings.test.ts
@@ -8,15 +8,12 @@ test("generated DevTools client sends one structured request", async () => {
     devtools: {
       setOverlay: (...args: unknown[]) => {
         calls.push(args);
-        return JSON.stringify({
-          ok: true,
-          value: {
-            layout: true,
-            clips: true,
-            hitTarget: true,
-            selectedNode: null,
-          },
-        });
+        return {
+          layout: true,
+          clips: true,
+          hitTarget: true,
+          selectedNode: null,
+        };
       },
     },
   } as unknown as Host);
@@ -31,12 +28,12 @@ test("generated DevTools client sends one structured request", async () => {
   ).resolves.toMatchObject({ layout: true });
   expect(calls).toEqual([
     [
-      JSON.stringify({
+      {
         layout: true,
         clips: true,
         hitTarget: true,
         selectedNode: null,
-      }),
+      },
     ],
   ]);
 });
@@ -47,19 +44,16 @@ test("generated no-request methods are argument-free at the native boundary", as
     devtools: {
       status: (...args: unknown[]) => {
         calls.push(args);
-        return JSON.stringify({
-          ok: true,
-          value: {
-            connected: true,
+        return {
+          connected: true,
+          selectedNode: null,
+          overlay: {
+            layout: false,
+            clips: false,
+            hitTarget: false,
             selectedNode: null,
-            overlay: {
-              layout: false,
-              clips: false,
-              hitTarget: false,
-              selectedNode: null,
-            },
           },
-        });
+        };
       },
     },
   } as unknown as Host);
diff --git a/apps/gallery/src/bindings.rs b/apps/gallery/src/bindings.rs
index 77b4537..571e98d 100644
--- a/apps/gallery/src/bindings.rs
+++ b/apps/gallery/src/bindings.rs
@@ -1,5 +1,5 @@
 use serde::{Deserialize, Serialize};
-use wabou::{Bindings, Capability, JsonMethod, Type, specta};
+use wabou::{Bindings, Capability, HostMethod, Type, specta};

 /// Capability containing the gallery's binding demonstration.
 pub const CAPABILITY: &str = "bindingsDemo";
@@ -32,8 +32,8 @@ pub enum DescribePaletteResponse {
 }

 /// Typed contract of the palette-description endpoint.
-pub const DESCRIBE_PALETTE: JsonMethod<DescribePaletteRequest, DescribePaletteResponse> =
-    JsonMethod::new("describePalette");
+pub const DESCRIBE_PALETTE: HostMethod<DescribePaletteRequest, DescribePaletteResponse> =
+    HostMethod::new("describePalette");

 /// Builds the gallery's typed binding manifest.
 pub fn manifest() -> Bindings {
@@ -60,7 +60,10 @@ mod tests {
     fn registered_method_and_manifest_share_names() {
         let output = manifest().render();
         assert!(output.contains(&format!("readonly {CAPABILITY}")));
-        assert!(output.contains(&format!("{}(request: string)", DESCRIBE_PALETTE.name())));
+        assert!(output.contains(&format!(
+            "{}(request: DescribePaletteRequest)",
+            DESCRIBE_PALETTE.name()
+        )));
         assert!(output.contains(
             "describePalette(request: DescribePaletteRequest): Promise<DescribePaletteResponse>"
         ));
diff --git a/apps/gallery/src/main.rs b/apps/gallery/src/main.rs
index fdd8faa..858bdd1 100644
--- a/apps/gallery/src/main.rs
+++ b/apps/gallery/src/main.rs
@@ -13,7 +13,7 @@ fn main() -> Result<(), Whatever> {
                 .initial_inner_size(1080, 760)
                 .min_inner_size(720, 520),
         )
-        .json_capability(gallery::bindings::CAPABILITY, |capability| {
+        .typed_capability(gallery::bindings::CAPABILITY, |capability| {
             capability.method(
                 gallery::bindings::DESCRIBE_PALETTE,
                 gallery::bindings::describe_palette,
diff --git a/apps/gallery/ui/generated/host-bindings.ts b/apps/gallery/ui/generated/host-bindings.ts
index ea4522e..11309c3 100644
--- a/apps/gallery/ui/generated/host-bindings.ts
+++ b/apps/gallery/ui/generated/host-bindings.ts
@@ -1,8 +1,6 @@
 // AUTO-GENERATED by wabou-bindgen. DO NOT EDIT.
 import { type Host, useHost } from "@wabou/core";

-export type NativeResult<T> = T | PromiseLike<T>;
-
 export /**  Input accepted by the palette-description example. */
 type DescribePaletteRequest = {
	/**  Base palette name. */
@@ -26,40 +24,26 @@ message: string };

 interface NativeHostCapabilities {
   readonly bindingsDemo: {
-    describePalette(request: string): NativeResult<string>;
+    describePalette(request: DescribePaletteRequest): Promise<DescribePaletteResponse>;
   };
 }

 export class NativeCapabilityError extends Error {
   readonly operation: string;
+  readonly nativeCause: unknown;

-  constructor(operation: string, message: string) {
+  constructor(operation: string, message: string, nativeCause?: unknown) {
     super(`${operation}: ${message}`);
     this.name = "NativeCapabilityError";
     this.operation = operation;
+    this.nativeCause = nativeCause;
   }
 }

-function decodeNativeResult<T>(raw: string, operation: string): T {
-  let result: unknown;
-  try {
-    result = JSON.parse(raw);
-  } catch (error) {
-    const detail = error instanceof Error ? error.message : String(error);
-    throw new NativeCapabilityError(operation, `invalid JSON response: ${detail}`);
-  }
-  if (typeof result !== "object" || result === null || typeof (result as { ok?: unknown }).ok !== "boolean") {
-    throw new NativeCapabilityError(operation, "invalid response envelope");
-  }
-  const envelope = result as { ok: true; value?: T } | { ok: false; error?: unknown };
-  if (!envelope.ok) {
-    const detail = typeof envelope.error === "string" ? envelope.error : "native capability failed";
-    throw new NativeCapabilityError(operation, detail);
-  }
-  if (!("value" in envelope)) {
-    throw new NativeCapabilityError(operation, "successful response is missing `value`");
-  }
-  return envelope.value as T;
+function asNativeCapabilityError(operation: string, error: unknown): NativeCapabilityError {
+  if (error instanceof NativeCapabilityError) return error;
+  const detail = error instanceof Error ? error.message : String(error);
+  return new NativeCapabilityError(operation, detail, error);
 }

 export interface BindingsDemoClient {
@@ -69,7 +53,13 @@ export interface BindingsDemoClient {
 export function createBindingsDemoClient(host: Host): BindingsDemoClient {
   const nativeHost = host as Host & NativeHostCapabilities;
   return {
-    describePalette: async (request) => decodeNativeResult<DescribePaletteResponse>(await nativeHost.bindingsDemo.describePalette(JSON.stringify(request)), "bindingsDemo.describePalette"),
+    describePalette: async (request) => {
+      try {
+        return await nativeHost.bindingsDemo.describePalette(request);
+      } catch (error) {
+        throw asNativeCapabilityError("bindingsDemo.describePalette", error);
+      }
+    },
   };
 }

diff --git a/apps/gallery/ui/host-bindings.test.ts b/apps/gallery/ui/host-bindings.test.ts
index dec09c3..86a6143 100644
--- a/apps/gallery/ui/host-bindings.test.ts
+++ b/apps/gallery/ui/host-bindings.test.ts
@@ -11,30 +11,28 @@ import {
 const resolve = (value: unknown): unknown =>
   typeof value === "function" ? resolve(value()) : value;

-test("generated client owns the JSON capability boundary", async () => {
-  let encoded = "";
+test("generated client passes typed values directly to the host", async () => {
+  let received: unknown;
   const host = {
     bindingsDemo: {
-      describePalette: async (request: string) => {
-        encoded = request;
-        return JSON.stringify({
-          ok: true,
-          value: {
-            status: "palette",
-            title: "Ocean palette",
-            swatches: ["Ocean-1", "Ocean-2"],
-          },
-        });
+      describePalette: async (request: unknown) => {
+        received = request;
+        return {
+          status: "palette",
+          title: "Ocean palette",
+          swatches: ["Ocean-1", "Ocean-2"],
+        };
       },
     },
   } as unknown as Host;

-  const response = await createBindingsDemoClient(host).describePalette({
+  const request = {
     name: "Ocean",
     swatchCount: 2,
-  });
+  };
+  const response = await createBindingsDemoClient(host).describePalette(request);

-  expect(JSON.parse(encoded)).toEqual({ name: "Ocean", swatchCount: 2 });
+  expect(received).toBe(request);
   expect(response).toEqual({
     status: "palette",
     title: "Ocean palette",
@@ -42,43 +40,32 @@ test("generated client owns the JSON capability boundary", async () => {
   });
 });

-test("generated client identifies malformed native responses", async () => {
+test("generated client adds operation context to transport failures", async () => {
+  const transportError = new Error("bridge disconnected");
   const client = createBindingsDemoClient({
     bindingsDemo: {
-      describePalette: () => "not JSON",
+      describePalette: () => Promise.reject(transportError),
     },
   } as unknown as Host);

   const error = await client
-    .describePalette({ name: "broken", swatchCount: 1 })
+    .describePalette({ name: "offline", swatchCount: 1 })
     .catch((reason: unknown) => reason);

   expect(error).toBeInstanceOf(NativeCapabilityError);
-  expect(error).toMatchObject({ operation: "bindingsDemo.describePalette" });
-  expect(String(error)).toContain(
-    "bindingsDemo.describePalette: invalid JSON response",
-  );
-});
-
-test("generated client rejects malformed native envelopes", async () => {
-  const client = createBindingsDemoClient({
-    bindingsDemo: {
-      describePalette: () => JSON.stringify({ status: "missing envelope" }),
-    },
-  } as unknown as Host);
-
-  await expect(
-    client.describePalette({ name: "broken", swatchCount: 1 }),
-  ).rejects.toThrow(
-    "bindingsDemo.describePalette: invalid response envelope",
+  expect(error).toMatchObject({
+    operation: "bindingsDemo.describePalette",
+    nativeCause: transportError,
+  });
+  expect(String(error)).toBe(
+    "NativeCapabilityError: bindingsDemo.describePalette: bridge disconnected",
   );
 });

 test("generated hook reads the current host context", async () => {
   const host = {
     bindingsDemo: {
-      describePalette: () =>
-        JSON.stringify({ ok: false, error: "context host reached" }),
+      describePalette: () => Promise.reject(new Error("context host reached")),
     },
   } as unknown as Host;
   let client: BindingsDemoClient | undefined;
diff --git a/crates/wabou-bindgen/src/lib.rs b/crates/wabou-bindgen/src/lib.rs
index ce3b579..8741b8f 100644
--- a/crates/wabou-bindgen/src/lib.rs
+++ b/crates/wabou-bindgen/src/lib.rs
@@ -2,10 +2,11 @@
 //!
 //! A native QuickJS method may return immediately or through a Promise.
 //! Generated clients normalize both forms to an asynchronous typed API and
-//! own JSON serialization so application code only sees typed DTOs.
+//! expose the direct serde-backed FFI contract so application code only sees
+//! typed DTOs.
 //!
 //! The default `generate` feature exposes Specta-backed TypeScript generation.
-//! Runtime crates may disable it and retain only [`JsonMethod`] plus the shared
+//! Runtime crates may disable it and retain only [`HostMethod`] plus the shared
 //! contract identifier validation, without pulling a language exporter into
 //! the application runtime graph.

@@ -32,28 +33,28 @@ use std::path::{Path, PathBuf};
 #[cfg(feature = "generate")]
 const HEADER: &str = "// AUTO-GENERATED by wabou-bindgen. DO NOT EDIT.\n";

-/// One typed JSON capability method shared by binding generation and runtime
+/// One typed in-process capability method shared by binding generation and runtime
 /// registration.
 ///
 /// The value is zero-sized apart from its static wire name. Its generic types
 /// make request/response drift a Rust compile error rather than a runtime
 /// bridge failure.
-pub struct JsonMethod<Request, Response> {
+pub struct HostMethod<Request, Response> {
     name: &'static str,
     #[cfg_attr(not(feature = "generate"), allow(dead_code))]
     has_request: bool,
     marker: PhantomData<fn(Request) -> Response>,
 }

-impl<Request, Response> Copy for JsonMethod<Request, Response> {}
+impl<Request, Response> Copy for HostMethod<Request, Response> {}

-impl<Request, Response> Clone for JsonMethod<Request, Response> {
+impl<Request, Response> Clone for HostMethod<Request, Response> {
     fn clone(&self) -> Self {
         *self
     }
 }

-impl<Request, Response> JsonMethod<Request, Response> {
+impl<Request, Response> HostMethod<Request, Response> {
     /// Define a method accepting one JSON request value.
     ///
     /// The directional serde bounds match the runtime wire contract: requests
@@ -62,11 +63,11 @@ impl<Request, Response> JsonMethod<Request, Response> {
     /// A schema-only request cannot accidentally become a wire contract:
     ///
     /// ```compile_fail
-    /// use wabou_bindgen::JsonMethod;
+    /// use wabou_bindgen::HostMethod;
     ///
     /// struct NotDeserializable;
-    /// const INVALID: JsonMethod<NotDeserializable, bool> =
-    ///     JsonMethod::new("invalid");
+    /// const INVALID: HostMethod<NotDeserializable, bool> =
+    ///     HostMethod::new("invalid");
     /// ```
     pub const fn new(name: &'static str) -> Self
     where
@@ -88,15 +89,26 @@ impl<Request, Response> JsonMethod<Request, Response> {
     /// Return whether the wire method accepts a JSON request argument.
     ///
     /// This keeps runtime registration and generated clients aligned for
-    /// [`JsonMethod::no_request`], whose native function truly has no
-    /// argument instead of accepting an undocumented JSON `null` sentinel.
+    /// [`HostMethod::no_request`], whose native function truly has no
+    /// argument instead of accepting an undocumented sentinel value.
     pub const fn has_request(self) -> bool {
         self.has_request
     }
 }

-impl<Response> JsonMethod<(), Response> {
+impl<Response> HostMethod<(), Response> {
     /// Define a method whose public client accepts no request value.
+    ///
+    /// A response that cannot cross the serde-backed FFI boundary is rejected
+    /// at the contract:
+    ///
+    /// ```compile_fail
+    /// use wabou_bindgen::HostMethod;
+    ///
+    /// struct NotSerializable;
+    /// const INVALID: HostMethod<(), NotSerializable> =
+    ///     HostMethod::no_request("invalid");
+    /// ```
     pub const fn no_request(name: &'static str) -> Self
     where
         Response: Serialize,
@@ -166,11 +178,11 @@ impl Argument {
     }

     fn native_parameter(&self) -> String {
-        format!("{}: string", self.name)
+        self.typed_parameter()
     }

     fn native_value(&self) -> String {
-        format!("JSON.stringify({})", self.name)
+        self.name.clone()
     }
 }

@@ -220,7 +232,7 @@ impl Capability {
     /// Add a typed method contract shared with runtime registration.
     pub fn method<Request: Type, Response: Type>(
         self,
-        method: JsonMethod<Request, Response>,
+        method: HostMethod<Request, Response>,
     ) -> Self {
         assert_identifier(method.name, "method");
         let mut request_types = Types::default();
@@ -272,8 +284,7 @@ pub struct Bindings {
 }

 /// A generated TypeScript interface for a flat native module. Unlike a
-/// capability client this preserves each Rust function's sync/async shape and
-/// does not impose Wabou's JSON result envelope.
+/// capability client this preserves each Rust function's sync/async shape.
 #[cfg(feature = "generate")]
 pub struct FunctionModule {
     name: String,
@@ -432,12 +443,10 @@ impl Bindings {
     }

     /// Render one deterministic module containing DTOs, a module-local native
-    /// capability contract, and typed JSON clients.
+    /// capability contract, and typed clients.
     pub fn render(&self) -> String {
         let mut output = String::from(HEADER);
         output.push_str("import { type Host, useHost } from \"@wabou/core\";\n\n");
-        output.push_str("export type NativeResult<T> = T | PromiseLike<T>;\n\n");
-
         let mut declarations = BTreeMap::new();
         for method in self
             .capabilities
@@ -454,8 +463,8 @@ impl Bindings {
             for method in sorted_methods(&capability.methods) {
                 let parameter = native_parameters(method);
                 output.push_str(&format!(
-                    "    {}({parameter}): NativeResult<string>;\n",
-                    method.name,
+                    "    {}({parameter}): Promise<{}>;\n",
+                    method.name, method.response
                 ));
             }
             output.push_str("  };\n");
@@ -468,7 +477,7 @@ impl Bindings {
             .any(|capability| !capability.methods.is_empty())
         {
             output.push_str(
-                "export class NativeCapabilityError extends Error {\n  readonly operation: string;\n\n  constructor(operation: string, message: string) {\n    super(`${operation}: ${message}`);\n    this.name = \"NativeCapabilityError\";\n    this.operation = operation;\n  }\n}\n\nfunction decodeNativeResult<T>(raw: string, operation: string): T {\n  let result: unknown;\n  try {\n    result = JSON.parse(raw);\n  } catch (error) {\n    const detail = error instanceof Error ? error.message : String(error);\n    throw new NativeCapabilityError(operation, `invalid JSON response: ${detail}`);\n  }\n  if (typeof result !== \"object\" || result === null || typeof (result as { ok?: unknown }).ok !== \"boolean\") {\n    throw new NativeCapabilityError(operation, \"invalid response envelope\");\n  }\n  const envelope = result as { ok: true; value?: T } | { ok: false; error?: unknown };\n  if (!envelope.ok) {\n    const detail = typeof envelope.error === \"string\" ? envelope.error : \"native capability failed\";\n    throw new NativeCapabilityError(operation, detail);\n  }\n  if (!(\"value\" in envelope)) {\n    throw new NativeCapabilityError(operation, \"successful response is missing `value`\");\n  }\n  return envelope.value as T;\n}\n\n",
+                "export class NativeCapabilityError extends Error {\n  readonly operation: string;\n  readonly nativeCause: unknown;\n\n  constructor(operation: string, message: string, nativeCause?: unknown) {\n    super(`${operation}: ${message}`);\n    this.name = \"NativeCapabilityError\";\n    this.operation = operation;\n    this.nativeCause = nativeCause;\n  }\n}\n\nfunction asNativeCapabilityError(operation: string, error: unknown): NativeCapabilityError {\n  if (error instanceof NativeCapabilityError) return error;\n  const detail = error instanceof Error ? error.message : String(error);\n  return new NativeCapabilityError(operation, detail, error);\n}\n\n",
             );
         }

@@ -496,13 +505,14 @@ impl Bindings {
                     .collect::<Vec<_>>()
                     .join(", ");
                 let argument = native_arguments(method);
-                let decode = format!(
-                    "decodeNativeResult<{}>(await nativeHost.{}.{}({argument}), \"{}.{}\")",
-                    method.response, capability.name, method.name, capability.name, method.name
+                let operation = format!("{}.{}", capability.name, method.name);
+                let invoke = format!(
+                    "await nativeHost.{}.{}({argument})",
+                    capability.name, method.name
                 );
                 output.push_str(&format!(
-                    "    {}: async ({parameter}) => {decode},\n",
-                    method.name,
+                    "    {}: async ({parameter}) => {{\n      try {{\n        return {invoke};\n      }} catch (error) {{\n        throw asNativeCapabilityError(\"{operation}\", error);\n      }}\n    }},\n",
+                    method.name
                 ));
             }
             output.push_str("  };\n}\n\n");
@@ -693,9 +703,16 @@ fn assert_identifier(value: &str, kind: &str) {
     );
 }

-/// Return whether `value` is a valid identifier for Wabou's generated
+/// Return whether `value` is a safe identifier for Wabou's generated
 /// TypeScript capability and method contracts.
+///
+/// Besides the ASCII identifier grammar, this rejects `__proto__`, whose
+/// assignment has prototype mutation semantics on ordinary JavaScript
+/// objects rather than behaving like a normal capability property.
 pub fn is_contract_identifier(value: &str) -> bool {
+    if value == "__proto__" {
+        return false;
+    }
     let mut chars = value.chars();
     chars
         .next()
@@ -714,7 +731,22 @@ fn render_datatype(
     let mapped_types = format
         .map_types(types)
         .unwrap_or_else(|error| panic!("cannot map capability {kind}: {error}"));
+    let semantics = ffi_semantics();
+    let mapped_types = semantics.apply_types(&mapped_types);
     let datatype = specta_serde::select_phase_datatype(datatype, &mapped_types, phase);
+    let datatype = match phase {
+        // Requests travel from JavaScript into Rust; responses travel from
+        // Rust into JavaScript. The semantic transform naming follows the
+        // frontend serialization direction used by Specta.
+        specta_serde::Phase::Deserialize => {
+            semantics.apply_serialize(&mapped_types, &datatype, "value")
+        }
+        specta_serde::Phase::Serialize => {
+            semantics.apply_deserialize(&mapped_types, &datatype, "value")
+        }
+    }
+    .and_then(|(datatype, _runtime)| datatype)
+    .unwrap_or(datatype);
     render_mapped_datatype(&mapped_types, &datatype)
         .unwrap_or_else(|error| panic!("cannot export capability {kind}: {error}"))
 }
@@ -739,6 +771,8 @@ fn specta_declarations(types: &Types, phase: specta_serde::Phase) -> BTreeMap<St
     let mapped = specta_serde::PhasesFormat
         .map_types(types)
         .unwrap_or_else(|error| panic!("cannot map Specta declarations: {error}"));
+    let semantics = ffi_semantics();
+    let mapped = semantics.apply_types(&mapped);
     let names = mapped
         .into_sorted_iter()
         .map(|datatype| datatype.name.to_string())
@@ -785,6 +819,15 @@ fn specta_declarations(types: &Types, phase: specta_serde::Phase) -> BTreeMap<St
     declarations
 }

+#[cfg(feature = "generate")]
+fn ffi_semantics() -> specta_typescript::semantic::Configuration {
+    // The in-process QuickJS bridge preserves NaN and infinities. Do not use
+    // Specta's JSON-oriented `number | null` float representation here.
+    // Other default semantic transforms (Date, URL, Bytes) are deliberately
+    // excluded until the FFI adapter produces those JavaScript runtime types.
+    specta_typescript::semantic::Configuration::empty().enable_lossless_floats()
+}
+
 #[cfg(feature = "generate")]
 fn upper_camel_case(value: &str) -> String {
     let mut result = String::new();
@@ -836,6 +879,32 @@ mod tests {
         server_owned: bool,
     }

+    #[derive(Deserialize, Type)]
+    #[serde(rename_all = "camelCase")]
+    struct RequestMetadata {
+        trace_id: String,
+    }
+
+    #[derive(Deserialize, Type)]
+    struct FlattenedRequest {
+        action: String,
+        #[serde(default)]
+        verbose: bool,
+        #[serde(flatten)]
+        metadata: RequestMetadata,
+    }
+
+    #[derive(Serialize, Type)]
+    #[serde(
+        tag = "status",
+        rename_all = "camelCase",
+        rename_all_fields = "camelCase"
+    )]
+    enum TaggedResponse {
+        Ready { item_count: u32 },
+        Unavailable,
+    }
+
     #[derive(Deserialize, Serialize, Type)]
     struct BidirectionalPayload {
         #[serde(skip_serializing_if = "Option::is_none")]
@@ -866,12 +935,12 @@ mod tests {
         beta: beta::Status,
     }

-    const UPDATE_FILE: JsonMethod<FunctionRequest, FunctionResponse> =
-        JsonMethod::new("updateFile");
-    const CURRENT_STATUS: JsonMethod<(), FunctionResponse> =
-        JsonMethod::no_request("currentStatus");
-    const OPTIONAL_UPDATE: JsonMethod<Option<FunctionRequest>, FunctionResponse> =
-        JsonMethod::new("optionalUpdate");
+    const UPDATE_FILE: HostMethod<FunctionRequest, FunctionResponse> =
+        HostMethod::new("updateFile");
+    const CURRENT_STATUS: HostMethod<(), FunctionResponse> =
+        HostMethod::no_request("currentStatus");
+    const OPTIONAL_UPDATE: HostMethod<Option<FunctionRequest>, FunctionResponse> =
+        HostMethod::new("optionalUpdate");

     fn fixture() -> Bindings {
         Bindings::new().capability(Capability::new("workspace").method(UPDATE_FILE))
@@ -882,15 +951,14 @@ mod tests {
         let output = fixture().render();
         assert!(output.contains("export type FunctionRequest ="));
         assert!(output.contains("displayName: string"));
-        assert!(output.contains("export type NativeResult<T> = T | PromiseLike<T>"));
+        assert!(!output.contains("NativeResult"));
         assert!(output.contains("interface NativeHostCapabilities"));
         assert!(!output.contains("@wabou/solid-renderer"));
-        assert!(output.contains("updateFile(request: string): NativeResult<string>"));
         assert!(output.contains("updateFile(request: FunctionRequest): Promise<FunctionResponse>"));
-        assert!(output.contains("JSON.stringify(request)"));
-        assert!(output.contains(
-            "decodeNativeResult<FunctionResponse>(await nativeHost.workspace.updateFile"
-        ));
+        assert!(output.contains("updateFile(request: FunctionRequest): Promise<FunctionResponse>"));
+        assert!(output.contains("await nativeHost.workspace.updateFile(request)"));
+        assert!(!output.contains("JSON.stringify"));
+        assert!(!output.contains("JSON.parse"));
         assert!(output.contains("\"workspace.updateFile\""));
         assert!(output.contains("useWorkspaceClient(): WorkspaceClient"));
         assert!(output.contains("createWorkspaceClient(useHost())"));
@@ -903,7 +971,7 @@ mod tests {
             .render();
         assert!(output.contains("export type FunctionRequest"));
         assert!(output.contains("updateFile(request: FunctionRequest): Promise<FunctionResponse>"));
-        assert!(output.contains("nativeHost.workspace.updateFile(JSON.stringify(request))"));
+        assert!(output.contains("nativeHost.workspace.updateFile(request)"));
     }

     #[test]
@@ -917,9 +985,8 @@ mod tests {
             .render();

         assert!(output.contains("updateFile(request: FunctionRequest)"));
-        assert!(output.contains("updateFile(request: string): NativeResult<string>"));
+        assert!(output.contains("updateFile(request: FunctionRequest): Promise<FunctionResponse>"));
         assert!(output.contains("currentStatus(): Promise<FunctionResponse>"));
-        assert!(output.contains("currentStatus(): NativeResult<string>"));
         assert!(output.contains("nativeHost.workspace.currentStatus()"));
         assert!(!output.contains("JSON.stringify(null)"));
     }
@@ -948,7 +1015,7 @@ mod tests {
     fn contract_identifier_grammar_is_explicit_and_shared() {
         assert!(is_contract_identifier("workspace"));
         assert!(is_contract_identifier("_workspace2"));
-        for invalid in ["", "1workspace", "work-space", "工作区"] {
+        for invalid in ["", "1workspace", "work-space", "工作区", "__proto__"] {
             assert!(!is_contract_identifier(invalid), "accepted {invalid:?}");
         }
     }
@@ -974,13 +1041,14 @@ mod tests {

     #[test]
     fn response_bindings_follow_serde_shape_and_explicit_number_contracts() {
-        const READ: JsonMethod<(), DirectionalResponse> = JsonMethod::no_request("read");
+        const READ: HostMethod<(), DirectionalResponse> = HostMethod::no_request("read");
         let output = Bindings::new()
             .capability(Capability::new("metrics").method(READ))
             .render();

         assert!(output.contains("detail?: string | null"));
-        assert!(output.contains("measurement: number | null"));
+        assert!(output.contains("measurement: number"));
+        assert!(!output.contains("measurement: number | null"));
         assert!(output.contains("finite_measurement: number"));
         assert!(output.contains("read(): Promise<DirectionalResponse_Serialize>"));
         assert!(
@@ -989,9 +1057,20 @@ mod tests {
         assert!(!output.contains("DirectionalResponse_Deserialize"));
     }

+    #[test]
+    fn direct_ffi_float_contracts_preserve_javascript_number_semantics() {
+        const ECHO: HostMethod<f64, f64> = HostMethod::new("echo");
+        let output = Bindings::new()
+            .capability(Capability::new("numbers").method(ECHO))
+            .render();
+
+        assert!(output.contains("echo(request: number): Promise<number>"));
+        assert!(!output.contains("number | null"));
+    }
+
     #[test]
     fn request_bindings_export_only_the_deserialization_shape() {
-        const WRITE: JsonMethod<DirectionalRequest, bool> = JsonMethod::new("write");
+        const WRITE: HostMethod<DirectionalRequest, bool> = HostMethod::new("write");
         let output = Bindings::new()
             .capability(Capability::new("metrics").method(WRITE))
             .render();
@@ -1007,8 +1086,8 @@ mod tests {

     #[test]
     fn bidirectional_contracts_keep_explicit_phase_names() {
-        const ROUND_TRIP: JsonMethod<BidirectionalPayload, BidirectionalPayload> =
-            JsonMethod::new("roundTrip");
+        const ROUND_TRIP: HostMethod<BidirectionalPayload, BidirectionalPayload> =
+            HostMethod::new("roundTrip");
         let output = Bindings::new()
             .capability(Capability::new("workspace").method(ROUND_TRIP))
             .render();
@@ -1018,11 +1097,29 @@ mod tests {
         assert!(!output.contains("type BidirectionalPayload ="));
     }

+    #[test]
+    fn serde_tagged_enums_and_flattening_define_the_wire_shape() {
+        const INSPECT: HostMethod<FlattenedRequest, TaggedResponse> = HostMethod::new("inspect");
+        let output = Bindings::new()
+            .capability(Capability::new("workspace").method(INSPECT))
+            .render();
+
+        assert!(output.contains("traceId: string"));
+        assert!(!output.contains("trace_id"));
+        assert!(output.contains("status: \"ready\""));
+        assert!(output.contains("status: \"unavailable\""));
+        assert!(output.contains("itemCount: number"));
+        assert!(!output.contains("item_count"));
+        assert!(output.contains("} & RequestMetadata"));
+        assert!(output.contains("verbose?: boolean"));
+        assert!(output.contains("inspect(request: FlattenedRequest)"));
+    }
+
     #[test]
     #[should_panic(expected = "conflicting TypeScript declaration for `Status`")]
     fn rejects_conflicting_dto_names_in_one_generated_module() {
-        const ALPHA: JsonMethod<(), alpha::Status> = JsonMethod::no_request("alpha");
-        const BETA: JsonMethod<(), beta::Status> = JsonMethod::no_request("beta");
+        const ALPHA: HostMethod<(), alpha::Status> = HostMethod::no_request("alpha");
+        const BETA: HostMethod<(), beta::Status> = HostMethod::no_request("beta");

         let _ = Bindings::new()
             .capability(Capability::new("first").method(ALPHA))
@@ -1033,7 +1130,7 @@ mod tests {
     #[test]
     #[should_panic(expected = "conflicting TypeScript declaration for `Status`")]
     fn rejects_conflicting_dto_names_inside_one_method_graph() {
-        const STATUS_PAIR: JsonMethod<ConflictingStatuses, ()> = JsonMethod::new("statusPair");
+        const STATUS_PAIR: HostMethod<ConflictingStatuses, ()> = HostMethod::new("statusPair");

         let _ = Bindings::new()
             .capability(Capability::new("workspace").method(STATUS_PAIR))
diff --git a/crates/wabou-runtime/Cargo.toml b/crates/wabou-runtime/Cargo.toml
index a43a4ea..bc0b34a 100644
--- a/crates/wabou-runtime/Cargo.toml
+++ b/crates/wabou-runtime/Cargo.toml
@@ -27,6 +27,7 @@ rquickjs = {
   version = "0.12.1",
   features = ["full-async", "loader", "rust-alloc"]
 }
+rquickjs-serde = "0.6.1"
 serde = { workspace = true, features = ["derive"] }
 serde_json = { workspace = true }
 snafu = { workspace = true }
diff --git a/crates/wabou-runtime/src/host.rs b/crates/wabou-runtime/src/host.rs
index cadadd0..73588a3 100644
--- a/crates/wabou-runtime/src/host.rs
+++ b/crates/wabou-runtime/src/host.rs
@@ -6,7 +6,7 @@
 //!
 //! ```ignore
 //! use serde::{Deserialize, Serialize};
-//! use wabou_bindgen::JsonMethod;
+//! use wabou_bindgen::HostMethod;
 //! use wabou_runtime::{HostBuilder, Widget};
 //!
 //! #[derive(Deserialize)]
@@ -21,8 +21,8 @@
 //!
 //! HostBuilder::new()
 //!     .widget("chart", || Box::new(MyChart::new()))
-//!     .json_capability("workspace", |capability| {
-//!         capability.method(JsonMethod::new("readFile"), |request: ReadFileRequest| async move {
+//!     .typed_capability("workspace", |capability| {
+//!         capability.method(HostMethod::new("readFile"), |request: ReadFileRequest| async move {
 //!             let contents = std::fs::read_to_string(request.path)
 //!                 .map_err(|error| error.to_string())?;
 //!             Ok::<_, String>(ReadFileResponse { contents })
@@ -44,7 +44,7 @@ use vello::peniko::Color;

 use serde::Serialize;
 use serde::de::DeserializeOwned;
-use wabou_bindgen::JsonMethod;
+use wabou_bindgen::HostMethod;

 use crate::HostMessageContext;
 use crate::applier::Applier;
@@ -58,23 +58,56 @@ type CapabilityInstaller = Arc<dyn Fn(&JsRuntime) -> rquickjs::Result<()>>;
 type HostMessageProducer = Arc<dyn Fn(HostMessageContext) + Send + Sync>;
 type WindowSource = (Box<dyn crate::FrameSource>, WindowOptions);

-/// A namespaced host capability that exposes structured asynchronous methods.
+struct SerdeJs<T>(T);
+
+impl<'js, T> rquickjs::FromJs<'js> for SerdeJs<T>
+where
+    T: DeserializeOwned,
+{
+    fn from_js(_ctx: &rquickjs::Ctx<'js>, value: rquickjs::Value<'js>) -> rquickjs::Result<Self> {
+        crate::ffi_value::from_value(value)
+            .map(Self)
+            .map_err(|error| {
+                rquickjs::Error::new_from_js_message(
+                    "JavaScript value",
+                    std::any::type_name::<T>(),
+                    error.to_string(),
+                )
+            })
+    }
+}
+
+impl<'js, T> rquickjs::IntoJs<'js> for SerdeJs<T>
+where
+    T: Serialize,
+{
+    fn into_js(self, ctx: &rquickjs::Ctx<'js>) -> rquickjs::Result<rquickjs::Value<'js>> {
+        crate::ffi_value::to_value(ctx.clone(), self.0).map_err(|error| {
+            rquickjs::Error::new_into_js_message(
+                std::any::type_name::<T>(),
+                "JavaScript value",
+                error.to_string(),
+            )
+        })
+    }
+}
+
+/// A namespaced host capability that exposes structured asynchronous methods
+/// through direct QuickJS values.
 ///
-/// Generated Wabou clients JSON-encode their single request value. This adapter
-/// owns the matching decode, Promise installation, and result envelope so
-/// applications do not duplicate transport glue around ordinary Rust async
-/// functions.
-pub struct JsonCapability<'js> {
+/// Serde defines the DTO shape, while `rquickjs-serde` converts that shape
+/// directly between Rust and QuickJS. No JSON text or result envelope is
+/// created for this in-process FFI boundary.
+pub struct TypedCapability<'js> {
     ctx: rquickjs::Ctx<'js>,
     object: rquickjs::Object<'js>,
 }

-impl<'js> JsonCapability<'js> {
-    /// Install one async method accepting a JSON-encoded request and returning
-    /// Wabou's `{ ok, value | error }` envelope.
+impl<'js> TypedCapability<'js> {
+    /// Install one async method accepting and returning direct QuickJS values.
     pub fn method<Request, Response, Error, Handler, HandlerFuture>(
         &self,
-        method: JsonMethod<Request, Response>,
+        method: HostMethod<Request, Response>,
         handler: Handler,
     ) -> rquickjs::Result<()>
     where
@@ -88,7 +121,7 @@ impl<'js> JsonCapability<'js> {
             return Err(rquickjs::Exception::throw_type(
                 &self.ctx,
                 &format!(
-                    "invalid JSON capability method identifier `{}`",
+                    "invalid typed capability method identifier `{}`",
                     method.name()
                 ),
             ));
@@ -96,15 +129,23 @@ impl<'js> JsonCapability<'js> {
         if self.object.contains_key(method.name())? {
             return Err(rquickjs::Exception::throw_type(
                 &self.ctx,
-                &format!("duplicate JSON capability method `{}`", method.name()),
+                &format!("duplicate typed capability method `{}`", method.name()),
             ));
         }
         if method.has_request() {
             let function = rquickjs::Function::new(
                 self.ctx.clone(),
-                rquickjs::prelude::Async(move |raw: String| {
+                rquickjs::prelude::Async(move |SerdeJs(request): SerdeJs<Request>| {
                     let handler = handler.clone();
-                    async move { invoke_json_method(&raw, handler).await }
+                    async move {
+                        handler(request).await.map(SerdeJs).map_err(|error| {
+                            rquickjs::Error::new_into_js_message(
+                                "Rust capability",
+                                "JavaScript Promise",
+                                error.to_string(),
+                            )
+                        })
+                    }
                 }),
             )?;
             self.object.set(method.name(), function)
@@ -113,7 +154,25 @@ impl<'js> JsonCapability<'js> {
                 self.ctx.clone(),
                 rquickjs::prelude::Async(move || {
                     let handler = handler.clone();
-                    async move { invoke_json_method("null", handler).await }
+                    async move {
+                        let request = <Request as serde::Deserialize>::deserialize(
+                            serde::de::value::UnitDeserializer::<serde::de::value::Error>::new(),
+                        )
+                        .map_err(|error| {
+                            rquickjs::Error::new_from_js_message(
+                                "empty capability request",
+                                std::any::type_name::<Request>(),
+                                error.to_string(),
+                            )
+                        })?;
+                        handler(request).await.map(SerdeJs).map_err(|error| {
+                            rquickjs::Error::new_into_js_message(
+                                "Rust capability",
+                                "JavaScript Promise",
+                                error.to_string(),
+                            )
+                        })
+                    }
                 }),
             )?;
             self.object.set(method.name(), function)
@@ -121,34 +180,6 @@ impl<'js> JsonCapability<'js> {
     }
 }

-async fn invoke_json_method<Request, Response, Error, Handler, HandlerFuture>(
-    raw: &str,
-    handler: Handler,
-) -> String
-where
-    Request: DeserializeOwned,
-    Response: Serialize,
-    Error: Display,
-    Handler: Fn(Request) -> HandlerFuture,
-    HandlerFuture: Future<Output = Result<Response, Error>>,
-{
-    let result = match serde_json::from_str(raw) {
-        Ok(request) => handler(request).await.map_err(|error| error.to_string()),
-        Err(error) => Err(format!("invalid capability request: {error}")),
-    };
-    match result {
-        Ok(value) => match serde_json::to_value(value) {
-            Ok(value) => serde_json::json!({ "ok": true, "value": value }).to_string(),
-            Err(error) => serde_json::json!({
-                "ok": false,
-                "error": format!("cannot encode capability response: {error}"),
-            })
-            .to_string(),
-        },
-        Err(error) => serde_json::json!({ "ok": false, "error": error }).to_string(),
-    }
-}
-
 struct ResourceCacheShutdownGuard {
     cache: Arc<ResourceCache>,
 }
@@ -302,16 +333,16 @@ impl HostBuilder {

     /// Mount structured async methods without exposing QuickJS transport
     /// details to application code.
-    pub fn json_capability<F>(self, name: impl Into<String>, mount: F) -> Self
+    pub fn typed_capability<F>(self, name: impl Into<String>, mount: F) -> Self
     where
-        F: for<'js> Fn(JsonCapability<'js>) -> rquickjs::Result<()>
+        F: for<'js> Fn(TypedCapability<'js>) -> rquickjs::Result<()>
             + rquickjs::markers::ParallelSend
             + Send
             + Sync
             + 'static,
     {
         self.capability(name, move |ctx, object| {
-            mount(JsonCapability { ctx, object })
+            mount(TypedCapability { ctx, object })
         })
     }

@@ -1028,9 +1059,9 @@ fn resource_bundle_candidates(executable: &Path) -> Vec<PathBuf> {
 #[cfg(test)]
 mod tests {
     use super::{
-        HostBuilder, JsonCapability, JsonMethod, install_host_message_producers,
-        invoke_json_method, resource_bundle_candidates, resource_bundle_path, test_environment,
-        test_report_summary, test_trace_artifact, write_test_artifact,
+        HostBuilder, HostMethod, TypedCapability, install_host_message_producers,
+        resource_bundle_candidates, resource_bundle_path, test_environment, test_report_summary,
+        test_trace_artifact, write_test_artifact,
     };
     use crate::host_message::{HostMessagePayload, host_message_channel};
     use crate::{Applier, HostMessageContext, JsRuntime};
@@ -1038,124 +1069,251 @@ mod tests {
     use std::sync::{Arc, Mutex};

     #[derive(serde::Deserialize)]
-    struct JsonRequest {
+    struct TypedRequest {
         value: u32,
     }

     #[derive(serde::Serialize)]
-    struct JsonResponse {
+    struct TypedResponse {
         doubled: u32,
     }

-    #[test]
-    fn json_capability_encodes_successful_results() {
-        let response = futures_lite::future::block_on(invoke_json_method(
-            r#"{"value":21}"#,
-            |request: JsonRequest| async move {
-                Ok::<_, String>(JsonResponse {
-                    doubled: request.value * 2,
-                })
-            },
-        ));
-
-        assert_eq!(
-            serde_json::from_str::<serde_json::Value>(&response).unwrap(),
-            serde_json::json!({ "ok": true, "value": { "doubled": 42 } })
-        );
+    #[derive(serde::Serialize)]
+    struct LargeIntegerResponse {
+        value: u64,
     }

-    #[test]
-    fn json_capability_encodes_handler_errors() {
-        let response = futures_lite::future::block_on(invoke_json_method(
-            r#"{"value":7}"#,
-            |_request: JsonRequest| async move {
-                Err::<JsonResponse, _>("palette unavailable".to_owned())
-            },
-        ));
-
-        assert_eq!(
-            serde_json::from_str::<serde_json::Value>(&response).unwrap(),
-            serde_json::json!({ "ok": false, "error": "palette unavailable" })
-        );
+    #[derive(serde::Deserialize)]
+    #[serde(rename_all = "camelCase")]
+    struct FfiMetadata {
+        trace_id: String,
     }

-    #[test]
-    fn json_capability_rejects_malformed_requests_before_calling_the_handler() {
-        let response = futures_lite::future::block_on(invoke_json_method(
-            r#"{"value":"wrong"}"#,
-            |_request: JsonRequest| async move {
-                panic!("malformed requests must not reach the handler");
-                #[allow(unreachable_code)]
-                Ok::<_, String>(JsonResponse { doubled: 0 })
-            },
-        ));
-        let response = serde_json::from_str::<serde_json::Value>(&response).unwrap();
+    #[derive(serde::Deserialize)]
+    struct FfiContractRequest {
+        action: String,
+        #[serde(default)]
+        verbose: bool,
+        #[serde(flatten)]
+        metadata: FfiMetadata,
+    }

-        assert_eq!(response["ok"], false);
-        assert!(
-            response["error"]
-                .as_str()
-                .unwrap()
-                .starts_with("invalid capability request:")
-        );
+    #[derive(serde::Serialize)]
+    #[serde(
+        tag = "status",
+        rename_all = "camelCase",
+        rename_all_fields = "camelCase"
+    )]
+    enum FfiContractResponse {
+        Ready { item_count: u32 },
     }

     #[test]
-    fn json_capability_mounts_structured_and_empty_requests_in_quickjs() {
+    fn typed_capability_passes_structured_values_directly_through_quickjs() {
         let runtime = JsRuntime::new().expect("runtime");
         runtime
             .mount_capability("typed", |ctx, object| {
-                let capability = JsonCapability { ctx, object };
+                let capability = TypedCapability { ctx, object };
                 capability.method(
-                    JsonMethod::new("double"),
-                    |request: JsonRequest| async move {
-                        Ok::<_, String>(JsonResponse {
+                    HostMethod::new("double"),
+                    |request: TypedRequest| async move {
+                        Ok::<_, String>(TypedResponse {
                             doubled: request.value * 2,
                         })
                     },
                 )?;
-                capability.method(JsonMethod::no_request("ping"), |(): ()| async move {
+                capability.method(HostMethod::no_request("ping"), |(): ()| async move {
                     Ok::<_, String>("pong")
                 })
             })
-            .expect("mount JSON capability");
+            .expect("mount typed capability");
         runtime
             .with(|ctx| {
                 ctx.eval::<(), _>(
                     r#"
                     globalThis.capabilityResult = undefined;
                     Promise.all([
-                      __wabou_capabilities.typed.double(JSON.stringify({ value: 9 })),
+                      __wabou_capabilities.typed.double({ value: 9 }),
                       __wabou_capabilities.typed.ping(),
-                    ]).then(values => globalThis.capabilityResult = JSON.stringify(values.map(JSON.parse)));
+                    ]).then(values => globalThis.capabilityResult = JSON.stringify(values));
                     "#,
                 )
             })
-            .expect("invoke JSON capability");
+            .expect("invoke typed capability");
         runtime.poll_async_runtime();
         let result = runtime
             .with(|ctx| ctx.eval::<Option<String>, _>("globalThis.capabilityResult"))
-            .expect("read JSON capability result")
+            .expect("read typed capability result")
             .expect("capability promise settled");

         assert_eq!(
             serde_json::from_str::<serde_json::Value>(&result).unwrap(),
             serde_json::json!([
-                { "ok": true, "value": { "doubled": 18 } },
-                { "ok": true, "value": "pong" },
+                { "doubled": 18 },
+                "pong",
             ])
         );
     }

     #[test]
-    fn json_capability_rejects_duplicate_method_registration() {
+    fn typed_capability_obeys_serde_contract_attributes() {
+        let runtime = JsRuntime::new().expect("runtime");
+        runtime
+            .mount_capability("contract", |ctx, object| {
+                TypedCapability { ctx, object }.method(
+                    HostMethod::new("inspect"),
+                    |request: FfiContractRequest| async move {
+                        assert_eq!(request.action, "inspect");
+                        assert_eq!(request.metadata.trace_id, "trace-1");
+                        assert!(!request.verbose, "serde default must be applied");
+                        Ok::<_, String>(FfiContractResponse::Ready { item_count: 3 })
+                    },
+                )
+            })
+            .expect("mount serde contract capability");
+        runtime
+            .with(|ctx| {
+                ctx.eval::<(), _>(
+                    r#"
+                    globalThis.serdeContractResult = undefined;
+                    __wabou_capabilities.contract.inspect({
+                      action: "inspect",
+                      traceId: "trace-1",
+                    }).then(value => globalThis.serdeContractResult = JSON.stringify(value));
+                    "#,
+                )
+            })
+            .expect("invoke serde contract capability");
+        runtime.poll_async_runtime();
+        let result = runtime
+            .with(|ctx| ctx.eval::<Option<String>, _>("globalThis.serdeContractResult"))
+            .expect("read serde contract result")
+            .expect("serde contract promise settled");
+
+        assert_eq!(
+            serde_json::from_str::<serde_json::Value>(&result).unwrap(),
+            serde_json::json!({ "status": "ready", "itemCount": 3 })
+        );
+    }
+
+    #[test]
+    fn typed_capability_rejects_invalid_values_and_handler_errors() {
+        let runtime = JsRuntime::new().expect("runtime");
+        runtime
+            .mount_capability("typed", |ctx, object| {
+                let capability = TypedCapability { ctx, object };
+                capability.method(
+                    HostMethod::new("double"),
+                    |_request: TypedRequest| async move {
+                        panic!("invalid values must not reach the Rust handler");
+                        #[allow(unreachable_code)]
+                        Ok::<_, String>(TypedResponse { doubled: 0 })
+                    },
+                )?;
+                capability.method(
+                    HostMethod::new("fail"),
+                    |_request: TypedRequest| async move {
+                        Err::<TypedResponse, _>("palette unavailable".to_owned())
+                    },
+                )
+            })
+            .expect("mount typed capability");
+        runtime
+            .with(|ctx| {
+                ctx.eval::<(), _>(
+                    r#"
+                    globalThis.capabilityErrors = undefined;
+                    Promise.allSettled([
+                      Promise.resolve().then(() => __wabou_capabilities.typed.double({ value: "wrong" })),
+                      __wabou_capabilities.typed.fail({ value: 1 }),
+                    ]).then(results => {
+                      globalThis.capabilityErrors = JSON.stringify(results.map(result =>
+                        result.status === "rejected" ? String(result.reason) : "resolved"
+                      ));
+                    });
+                    "#,
+                )
+            })
+            .expect("invoke failing typed capabilities");
+        runtime.poll_async_runtime();
+        let result = runtime
+            .with(|ctx| ctx.eval::<Option<String>, _>("globalThis.capabilityErrors"))
+            .expect("read typed capability errors")
+            .expect("capability promises settled");
+        let errors = serde_json::from_str::<Vec<String>>(&result).unwrap();
+
+        assert_ne!(errors[0], "resolved");
+        assert!(
+            errors[0].contains("u32"),
+            "unexpected conversion error: {}",
+            errors[0]
+        );
+        assert!(
+            errors[1].contains("palette unavailable"),
+            "unexpected handler error: {}",
+            errors[1]
+        );
+    }
+
+    #[test]
+    fn typed_capability_preserves_floats_and_rejects_unsafe_integers() {
+        let runtime = JsRuntime::new().expect("runtime");
+        runtime
+            .mount_capability("numbers", |ctx, object| {
+                let capability = TypedCapability { ctx, object };
+                capability.method(HostMethod::no_request("nan"), |(): ()| async move {
+                    Ok::<_, String>(f64::NAN)
+                })?;
+                capability.method(HostMethod::no_request("large"), |(): ()| async move {
+                    Ok::<_, String>(LargeIntegerResponse {
+                        value: (1_u64 << 53) + 1,
+                    })
+                })
+            })
+            .expect("mount numeric capability");
+        runtime
+            .with(|ctx| {
+                ctx.eval::<(), _>(
+                    r#"
+                    globalThis.numericCapabilityResult = undefined;
+                    Promise.allSettled([
+                      __wabou_capabilities.numbers.nan(),
+                      __wabou_capabilities.numbers.large(),
+                    ]).then(([nan, large]) => {
+                      globalThis.numericCapabilityResult = JSON.stringify({
+                        nan: nan.status === "fulfilled" && Number.isNaN(nan.value),
+                        large: large.status === "rejected" ? String(large.reason) : "resolved",
+                      });
+                    });
+                    "#,
+                )
+            })
+            .expect("invoke numeric capabilities");
+        runtime.poll_async_runtime();
+        let result = runtime
+            .with(|ctx| ctx.eval::<Option<String>, _>("globalThis.numericCapabilityResult"))
+            .expect("read numeric capability result")
+            .expect("numeric capability promises settled");
+        let result = serde_json::from_str::<serde_json::Value>(&result).unwrap();
+
+        assert_eq!(result["nan"], true);
+        assert!(
+            result["large"]
+                .as_str()
+                .expect("large integer rejection")
+                .contains("safe integer range")
+        );
+    }
+
+    #[test]
+    fn typed_capability_rejects_duplicate_method_registration() {
         let runtime = JsRuntime::new().expect("runtime");
         let result = runtime.mount_capability("typed", |ctx, object| {
-            let capability = JsonCapability { ctx, object };
-            capability.method(JsonMethod::no_request("ping"), |(): ()| async move {
+            let capability = TypedCapability { ctx, object };
+            capability.method(HostMethod::no_request("ping"), |(): ()| async move {
                 Ok::<_, String>("first")
             })?;
-            capability.method(JsonMethod::no_request("ping"), |(): ()| async move {
+            capability.method(HostMethod::no_request("ping"), |(): ()| async move {
                 Ok::<_, String>("second")
             })
         });
@@ -1164,16 +1322,18 @@ mod tests {
     }

     #[test]
-    fn json_capability_uses_the_shared_method_identifier_rules() {
+    fn typed_capability_uses_the_shared_method_identifier_rules() {
         let runtime = JsRuntime::new().expect("runtime");
-        let result = runtime.mount_capability("typed", |ctx, object| {
-            JsonCapability { ctx, object }
-                .method(JsonMethod::no_request("1ping"), |(): ()| async move {
-                    Ok::<_, String>("pong")
-                })
-        });
+        for invalid in ["1ping", "__proto__"] {
+            let result = runtime.mount_capability("typed", |ctx, object| {
+                TypedCapability { ctx, object }
+                    .method(HostMethod::no_request(invalid), |(): ()| async move {
+                        Ok::<_, String>("pong")
+                    })
+            });

-        assert!(result.is_err());
+            assert!(result.is_err(), "accepted invalid method {invalid:?}");
+        }
     }

     #[cfg(feature = "profiling")]
diff --git a/crates/wabou-runtime/src/jsrt/tests.rs b/crates/wabou-runtime/src/jsrt/tests.rs
index f02901c..3ef0e34 100644
--- a/crates/wabou-runtime/src/jsrt/tests.rs
+++ b/crates/wabou-runtime/src/jsrt/tests.rs
@@ -99,7 +99,7 @@ fn mounted_capabilities_are_namespaced_and_reject_duplicates() {
 #[test]
 fn capability_names_use_the_generated_contract_identifier_rules() {
     let runtime = JsRuntime::new().expect("runtime");
-    for invalid in ["", "1workspace", "work-space", "工作区"] {
+    for invalid in ["", "1workspace", "work-space", "工作区", "__proto__"] {
         assert!(
             runtime
                 .mount_capability(invalid, |_ctx, _capability| Ok(()))
diff --git a/crates/wabou-runtime/src/lib.rs b/crates/wabou-runtime/src/lib.rs
index ea81740..978f743 100644
--- a/crates/wabou-runtime/src/lib.rs
+++ b/crates/wabou-runtime/src/lib.rs
@@ -14,6 +14,7 @@ mod clock;
 mod config;
 mod effect_trace;
 mod error;
+mod ffi_value;
 mod host;
 #[cfg(test)]
 pub(crate) mod host_abi;
diff --git a/crates/wabou/src/lib.rs b/crates/wabou/src/lib.rs
index e65df18..ce5ae97 100644
--- a/crates/wabou/src/lib.rs
+++ b/crates/wabou/src/lib.rs
@@ -14,7 +14,7 @@
 //! # }
 //! ```

-pub use wabou_bindgen::JsonMethod;
+pub use wabou_bindgen::HostMethod;
 #[cfg(feature = "bindings")]
 pub use wabou_bindgen::{Bindings, Capability, Type, specta};
 pub use wabou_runtime::*;
@@ -29,7 +29,7 @@ mod tests {
         let _window = WindowOptions::new().title("Facade test");
         let _: Option<&dyn Widget> = None;
         let _: Option<widget_api::UiEvent> = None;
-        let _: JsonMethod<(), bool> = JsonMethod::no_request("ready");
+        let _: HostMethod<(), bool> = HostMethod::no_request("ready");
     }

     #[cfg(feature = "bindings")]
diff --git a/crates/wabou/tests/bindings_facade.rs b/crates/wabou/tests/bindings_facade.rs
index eb922ee..0af1a67 100644
--- a/crates/wabou/tests/bindings_facade.rs
+++ b/crates/wabou/tests/bindings_facade.rs
@@ -1,6 +1,6 @@
 #![cfg(feature = "bindings")]

-use wabou::{Bindings, Capability, JsonMethod, Type, specta};
+use wabou::{Bindings, Capability, HostMethod, Type, specta};

 #[allow(dead_code)]
 #[derive(serde::Deserialize, serde::Serialize, Type)]
@@ -8,7 +8,7 @@ struct Payload {
     ready: bool,
 }

-const STATUS: JsonMethod<Payload, Payload> = JsonMethod::new("status");
+const STATUS: HostMethod<Payload, Payload> = HostMethod::new("status");

 #[test]
 fn downstream_code_can_derive_and_generate_through_the_facade() {
diff --git a/docs/api-design.md b/docs/api-design.md
index 48de5e1..3006cdf 100644
--- a/docs/api-design.md
+++ b/docs/api-design.md
@@ -107,27 +107,34 @@ declare module "@wabou/core/registry" {

 Rust remains the source of truth for serialized request, response, event DTOs
 and exported method contracts. Every application capability method is one
-`JsonMethod<Request, Response>` constant shared by bindgen and runtime
+`HostMethod<Request, Response>` constant shared by bindgen and runtime
 registration. Wabou pins the validated Specta 2 RC and uses `specta-serde`
 only to reflect its explicitly annotated bridge DTOs; it does not maintain
 fake contract-only functions. Request types are exported using their serde
-deserialization shape,
-while response types use their serialization shape. This preserves directional
-attributes such as `skip_serializing_if` instead of silently publishing an
-incorrect symmetric contract. The generator omits the opposite phase and
+deserialization shape, while response types use their serialization shape.
+This preserves `default`, `flatten`, tagged enums, field and variant renaming,
+and directional attributes such as `skip_serializing_if` instead of silently
+publishing an incorrect symmetric contract. The generator omits the opposite
+phase and
 Specta's synthetic two-phase wrapper unless that direction is actually used by
 another method. `wabou-bindgen` adds the capability namespace and Wabou wire
-policy, then generates a module-local raw contract, an injectable typed client
+policy, then generates a module-local native contract, an injectable typed client
 factory, and a `useXClient()` context consumer. It intentionally does not claim
-that the raw JSON methods merged into `Host` are the typed public API. Each method has zero or one
-request value, encoded as JSON (`null` when absent); sync and Promise-like
-native results are normalized at the client boundary.
-
-Specta 2 deliberately exports unconstrained Rust floats as `number | null`,
-matching `serde_json` for NaN and infinity. A DTO may opt into
-`specta_typescript::Number` only when its producer enforces a finite-value
-invariant. Framework metrics and geometry do this explicitly; bindgen does not
-globally weaken user DTO contracts.
+that internal capability registration is the public API. Each method has zero
+or one request value. Serde-backed adapters convert DTOs directly between Rust
+and QuickJS values; no-request methods are argument-free at both boundaries.
+Sync and Promise-like native results are normalized at the client boundary.
+Native failures and rejected bridge calls become `NativeCapabilityError`
+values carrying the failing
+`capability.method`, rather than leaking context-free transport errors.
+
+Wabou enables Specta 2's lossless-float semantics for this in-process FFI
+boundary. Rust floats therefore export as JavaScript `number` and preserve
+`NaN`, `Infinity`, and `-Infinity`; they are not converted to JSON `null`.
+Large Rust integer types remain forbidden by default because JavaScript number
+cannot represent them losslessly. A DTO may opt into
+`specta_typescript::Number` only when its producer enforces the JavaScript safe
+integer range.

 Do not infer the public API by parsing arbitrary `rquickjs::Function` closures.
 Their captured state, argument conversion, error policy, and Promise behavior
diff --git a/docs/architecture.md b/docs/architecture.md
index e0d9687..afd809f 100644
--- a/docs/architecture.md
+++ b/docs/architecture.md
@@ -59,10 +59,17 @@ features must have one authoritative declaration and generated Rust/TypeScript
 views; handwritten parallel enums or registration lists are drift bugs.

 The runtime and default `wabou` facade consume only the lightweight
-`JsonMethod` contract. Specta and the TypeScript exporter are behind
+`HostMethod` contract. Specta and the TypeScript exporter are behind
 `wabou-bindgen`'s `generate` and the facade's `bindings` features, so executing
 an application does not inherently depend on code-generation machinery.

+An extension API is not synonymous with a JSON protocol. In-process structured
+host methods use direct QuickJS FFI values with serde-backed conversion;
+per-frame mutations use the binary operation protocol; large resources use
+handles or byte buffers. JSON remains an application data format and may be an
+adapter for a future out-of-process plugin, but it is not Wabou's default
+in-process extension transport.
+
 The protocol transports explicit facts. For example, JS sends focusability and
 focus order as an interaction policy. Rust validates and applies that policy;
 it does not derive focusability from a button-like role.
diff --git a/docs/cli.md b/docs/cli.md
index 8fabe1f..8568791 100644
--- a/docs/cli.md
+++ b/docs/cli.md
@@ -230,15 +230,14 @@ run it in CI.

 The generated module exposes a typed client while retaining the native
 QuickJS ABI underneath. Each capability method accepts zero or one request;
-the bridge JSON-encodes methods that have a request. A `no_request` method is
-also argument-free at the native boundary; it does not rely on a hidden `null`
-sentinel. Group
-multiple fields into a named DTO. Application code does not manually call
-`JSON.stringify` or decode the native result envelope.
+serde-backed adapters convert request and response DTOs directly between Rust
+and QuickJS values. A `no_request` method is also argument-free at the native
+boundary. Group multiple fields into a named DTO. No JSON text or generic RPC
+envelope is created for this in-process call.

 For a Rust-owned capability, derive `specta::Type` on its bridge DTOs and define
 one typed method constant. The same constant is consumed by binding generation
-and runtime registration. `JsonMethod::new` requires the request to implement
+and runtime registration. `HostMethod::new` requires the request to implement
 `Deserialize` and the response to implement `Serialize`, so a schema that
 cannot cross the actual wire fails where the contract is declared:

@@ -251,15 +250,15 @@ Applications that do not generate Rust-owned TypeScript capabilities leave the
 feature disabled and do not compile Specta or the TypeScript exporter.

 ```rust
-use wabou::{Capability, JsonMethod, Type, specta};
+use wabou::{Capability, HostMethod, Type, specta};

 #[derive(serde::Deserialize, Type)]
 struct RenameRequest {
     name: String,
 }

-const RENAME: JsonMethod<RenameRequest, bool> = JsonMethod::new("rename");
-const STATUS: JsonMethod<(), WorkspaceStatus> = JsonMethod::no_request("status");
+const RENAME: HostMethod<RenameRequest, bool> = HostMethod::new("rename");
+const STATUS: HostMethod<(), WorkspaceStatus> = HostMethod::no_request("status");

 let capability = Capability::new("workspace")
     .method(RENAME)
@@ -273,17 +272,17 @@ its derive macros require the downstream crate to link `serde` by name.

 This generates DTO declarations, a module-local raw host contract, injectable
 `createXClient(host)` factories, and `useXClient()` context consumers from the
-shared contracts. It does not expose JSON wire methods as public typed host
-methods. Only explicit bridge DTOs should derive `Type`; persistence entities,
+shared contracts. It does not expose internal registration objects as the
+public Host API. Only explicit bridge DTOs should derive `Type`; persistence entities,
 upstream SDK models and renderer internals are not automatically public
 JavaScript contracts.

 Register structured async implementations through the matching runtime
-adapter. It owns JSON decoding, native Promise creation and the standard result
-envelope, so application code does not repeat the wire protocol:
+adapter. It owns serde conversion and native Promise creation, so application
+code does not repeat FFI glue:

 ```rust
-HostBuilder::new().json_capability("workspace", |capability| {
+HostBuilder::new().typed_capability("workspace", |capability| {
     capability.method(RENAME, rename)
 });
 ```
diff --git a/docs/host-to-js.md b/docs/host-to-js.md
index 12547a1..0d58c37 100644
--- a/docs/host-to-js.md
+++ b/docs/host-to-js.md
@@ -267,14 +267,14 @@ separate bounded stream API with explicit credits.
 ## User Rust capabilities

 User Rust APIs are mounted as named capabilities. Structured async operations
-use `json_capability`, which matches the client generated by `wabou-bindgen` and
-owns request decoding, Promise creation, and result-envelope encoding:
+use `typed_capability`, which matches the client generated by `wabou-bindgen` and
+owns direct serde-to-QuickJS conversion and Promise creation:

 ```rust
-const READ_FILE: JsonMethod<ReadFileRequest, ReadFileResponse> =
-    JsonMethod::new("readFile");
+const READ_FILE: HostMethod<ReadFileRequest, ReadFileResponse> =
+    HostMethod::new("readFile");

-HostBuilder::new().json_capability("workspace", |capability| {
+HostBuilder::new().typed_capability("workspace", |capability| {
     capability.method(READ_FILE, read_file)
 });
 ```
