# Component gallery

Interactive catalogue for `@wabou/components` and Wabou platform features,
including animation, native windows, and Rust custom widgets.

```bash
mise exec -- bun run wabou dev --app-dir apps/gallery
```
