// AUTO-GENERATED by wabou-bindgen. DO NOT EDIT.
export /**  A Gregorian calendar date without a time or time zone. */
type CalendarDateInfo = {
	/**  Proleptic Gregorian year. */
	year: number,
	/**  One-based month. */
	month: number,
	/**  One-based day of month. */
	day: number,
};

export /**  Timing and scene-size metrics for the most recently presented frame. */
type FrameStats = {
	/**  Total Rust frame construction time in milliseconds. */
	build_frame_ms: number,
	/**  QuickJS animation-frame callback time in milliseconds. */
	js_tick_ms: number,
	/**  Vello scene construction time in milliseconds. */
	scene_ms: number,
	/**  Surface rendering and presentation time in milliseconds. */
	present_ms: number,
	/**  Number of retained nodes in the frame. */
	node_count: number,
	/**  Logical viewport width. */
	viewport_w: number,
	/**  Logical viewport height. */
	viewport_h: number,
};

export /**  Layout and effective clipping geometry for one Solid node. */
type LayoutNodeMetrics = {
	/**  Solid-side node identifier. */
	id: number,
	/**  Border box in logical window coordinates. */
	rect: LayoutRect,
	/**  Effective ancestor clip in logical window coordinates. */
	clip: LayoutRect,
};

export /**  Axis-aligned rectangle in logical window coordinates. */
type LayoutRect = {
	/**  Left edge. */
	x: number,
	/**  Top edge. */
	y: number,
	/**  Non-negative width. */
	width: number,
	/**  Non-negative height. */
	height: number,
};

export /**  Immutable layout projection returned by the synchronous host API. */
type LayoutSnapshot = {
	/**  Monotonic layout revision used to detect stale snapshots. */
	revision: number,
	/**  Current logical viewport. */
	viewport: LayoutRect,
	/**  Metrics for the requested node identifiers that still exist. */
	nodes: LayoutNodeMetrics[],
};

export interface NativeHostApi {
  frameStats(): FrameStats | null;
  layoutSnapshot(ids: number[]): LayoutSnapshot;
  loadFont(path: string): boolean;
  openUrl(url: string): boolean;
  systemCalendarDate(): CalendarDateInfo;
  systemLocale(): string;
  systemTimeZone(): string;
}
